// import app from './app';
// import config from './config/config';

// const server = app.listen(config.PORT)

// ;(()=>{
//     try {
//         console.info(`Application started on ${config.PORT}, URL:${config.SERVER_URL}`)
//     } catch (error) {
//         console.error(error)

//         server.close((err) => {
//             if(err) console.error(err)

//             process.exit(1)
//         })
//     }
// })()
